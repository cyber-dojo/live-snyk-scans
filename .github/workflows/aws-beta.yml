name: Live Snyk Test aws-beta

on:
  #workflow_dispatch:
  push:
  #schedule: # At 08:00 every day
  #  - cron: '0 8 * * *'

env:
  KOSLI_ENV: aws-beta
  KOSLI_FLOW: aws-beta-synk-vuln-ages
  KOSLI_CUSTOM_ATTESTATION_TYPE: aws-beta-synk-vuln-ages
  KOSLI_POLICY_NAME: aws-beta-synk-vuln-ages
  KOSLI_POLICY_FILEPATH: aws-beta-synk-vuln-ages.yml

jobs:

  setup:
    runs-on: ubuntu-latest
    outputs:
      kosli_env: ${KOSLI_ENV}
      kosli_flow: ${KOSLI_FLOW}
      kosli_cat: ${KOSLI_CUSTOM_ATTESTATION_TYPE}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Kosli CLI
        uses: kosli-dev/setup-cli-action@v2
        with:
          version: ${{vars.KOSLI_CLI_VERSION}}

      - name: Create custom-attestation-type
        run:
          kosli create attestation-type 
            ${KOSLI_CUSTOM_ATTESTATION_TYPE}
            
      - name: Create environment-policy
        run: |
          echo TODO
          #kosli create policy 
          #  ${KOSLI_POLICY_NAME}
          #  ${KOSLI_POLICY_FILEPATH}

      - name: Attach custom-attestation-type to Environment
        run: |
          echo TODO
          #kosli attach-policy ${KOSLI_POLICY_NAME}
          #  --environment ${KOSLI_ENV}

  scan-aws-beta:
    needs: setup
    uses: ./.github/workflows/env_snyk_test.yml
    with:
      kosli_env: ${{need.setup.outputs.kosli_env}}
      kosli_flow: ${{needs.setup.outputs.kosli_flow}}
      kosli_cat: ${{needs.setup.outputs.kosli_cat}}
    secrets:
      snyk_token:      ${{secrets.SNYK_TOKEN}}
      kosli_api_token: ${{secrets.KOSLI_API_TOKEN}}
